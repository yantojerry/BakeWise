# BakeWise

A bakery management system for inventory, production, products, POS, reports, users, and settings.

## Setup

1. Install Python dependencies:

```powershell
pip install -r requirements.txt
```

2. Copy `.env.example` to `.env` and fill in your database values.

3. Run the app:

```powershell
python app.py
```
